pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                if (fileExists('file')) {
                  echo 'Yes'
                    } else {
                    echo 'No'
                    }
            }
        }
    }
}
